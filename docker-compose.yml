version: '3.8'

services:
  staylo-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: staylo-frontend
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - VITE_API_URL=http://localhost:8081/api
