// This file now imports and re-exports the Navbar component for backward compatibility
import Navbar from './Navbar';

// Re-export Navbar as Header for backward compatibility
const Header = Navbar;
// Export the Navbar component as Header for backward compatibility
export default Header;